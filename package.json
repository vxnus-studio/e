{
  "name": "e-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": { "build": "tsc -p packages/protocol && tsc -p packages/registry && tsc -p packages/knowledge", "test": "npm run build && node scripts/test-manifests.mjs && node scripts/test-knowledge.mjs" },
  "devDependencies": { "typescript": "^5.9.3" }
}
