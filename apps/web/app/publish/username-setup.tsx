"use client";
import { FormEvent, useState } from "react";
export function UsernameSetup() { const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setMessage(null); const response = await fetch("/api/profile", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const result = await response.json() as { message?: string }; if (!response.ok) setMessage(result.message || "Username could not be saved."); else window.location.reload(); setBusy(false); } return <section className="dashboard-upload"><div><span className="dashboard-kicker">Publisher identity</span><h2>Choose your username.</h2><p>Your username becomes your unique publisher ID and package namespace.</p></div><form className="publish-form" onSubmit={submit}><label className="provider-url-field">Username<input name="username" required pattern="[a-z0-9][a-z0-9-]{1,30}" minLength={2} maxLength={31} placeholder="your-name" /><small>Used in package names such as @your-name/knowledge.</small></label>{message && <p className="publish-status error">{message}</p>}<button className="button button-primary" disabled={busy}>{busy ? "Saving…" : "Save username"}<span>↗</span></button></form></section>; }
