# Supabase public client configuration
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase database connection (server-only; use the pooled connection string)
DATABASE_URL=
# Supabase Auth is the active session adapter.
SUPABASE_AUTH_ENABLED=true

# Immutable pack artifacts (Cloudflare R2 / S3-compatible API)
R2_ACCOUNT_ID=
R2_BUCKET_NAME=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_PUBLIC_BASE_URL=https://knowledge.e.vxnus.xyz
