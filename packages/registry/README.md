# @vxnus/e-registry

The domain contract for the hosted E Knowledge Hub registry. It describes
discoverable packs, distributions, search, and publisher verification.

This package does not contain a database, web framework, authentication
system, or hosting application. Those are implementation choices for the Hub.
