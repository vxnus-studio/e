# @vxnus/e-client

Client-side contracts and transport-neutral wrappers for the E Knowledge Hub
registry and remote knowledge providers. HTTP, authentication, retries, and
runtime policy are supplied by the host application.
